# vanilla 🍦

A tiny, responsive browser reset for [functional css](#).

```bash
npm install --save @rosszurowski/vanilla
```

I was using this code a lot, so I wanted to make it a module. Use it if you want, don't if you don't.
